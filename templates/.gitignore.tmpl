# Generated by vibe_bootstrap setup.py — customize as needed.

# --- Claude Code ---
# Machine-specific permission grants (the team-shared file is settings.json)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# --- Environment & Secrets ---
.env
.env.local
.env.*.local
!.env.example

# --- Editors / OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

{{#if backend}}
# --- Backend ({{backend.language}}) ---
__pycache__/
*.py[cod]
*.pyc
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
{{/if}}
{{#if frontend}}
# --- Frontend ({{frontend.framework}}) ---
node_modules/
dist/
build/
.next/
.expo/
.expo-shared/
.turbo/
*.tsbuildinfo
{{/if}}
{{#if frontend.is_native}}
# --- Expo / React Native ---
# Prebuild output and native projects are gitignored by default in managed
# Expo workflows. Remove these entries if you've ejected to bare workflow
# and want to track the native dirs.
{{frontend.directory}}/ios/
{{frontend.directory}}/android/
{{frontend.directory}}/web-build/
*.orig.*
# EAS local credentials
credentials.json
{{/if}}
{{#if deploy}}
# --- Deployment artifacts ---
VERSION
{{/if}}

# --- Logs ---
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# --- Certificates & Keys ---
*.key
*.pem
*.p12
*.pfx
*.crt
